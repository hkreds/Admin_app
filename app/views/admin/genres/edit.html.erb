<h1 class="px-3" style= "width: fit-content; background-color: #e6e6e6;">ジャンル編集</h1>
<%= form_with model: [:admin, @genre] do |f| %>
  <p>ジャンル名</p>
  <%= f.text_field :name %>
  <%= f.submit "変更を保存", class: "btn btn-success" %>
<% end %>